task test(type: Exec) {
	workingDir project.rootDir
	commandLine 'cmd', '/c', file('test-db.bat'), project(':android').projectDir
}

task check(dependsOn: test) {
}

task clean(type: Delete) {
	delete 'build'
}

tasks.all { it.shouldRunAfter tasks.clean }
