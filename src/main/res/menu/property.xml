<?xml version="1.0" encoding="utf-8"?>
<menu
	xmlns:android="http://schemas.android.com/apk/res/android"
	xmlns:app="http://schemas.android.com/apk/res-auto"
	>
	<item
		android:id="@+id/action_property_edit"
		android:title="@string/property_edit"
		android:icon="@android:drawable/ic_menu_edit"
		app:showAsAction="ifRoom|withText"
		/>
	<item
		android:id="@+id/action_property_delete"
		android:title="@string/property_delete"
		android:icon="@android:drawable/ic_menu_delete"
		app:showAsAction="never"
		/>
	<item
		android:id="@+id/action_property_sunburst"
		android:title="@string/sunburst_title"
		android:icon="@android:drawable/ic_menu_compass"
		app:showAsAction="never"
		/>
	<item
		android:id="@+id/action_share"
		android:title="@string/property_share"
		android:icon="@android:drawable/ic_menu_share"
		android:visible="@bool/in_test"
		app:showAsAction="never"
		/>
</menu>
