<resources>
	<string name="query_properties"><![CDATA["
		select
			p._id,
			p.name,
			'android.resource://net.twisterrob.inventory/drawable/' || pt.image as image
		from Property p
		join PropertyType pt ON p.type = pt._id
		order by p.name 
	"]]></string>
</resources>
