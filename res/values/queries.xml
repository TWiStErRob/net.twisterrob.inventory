<resources>
	<string name="query_properties"><![CDATA["
		select
			p.id as _id,
			p.name,
			pt.image
		from Property p
		join PropertyType pt ON p.type = pt.id
		order by p.name asc
	"]]></string>
	<string name="query_property"><![CDATA["
		select id as _id, name, type
		from Property
		where id = ?
	"]]></string>
	<string name="query_property_new"><![CDATA["
		insert into Property(name, type) values(?, ?)
	"]]></string>
	<string name="query_property_update"><![CDATA["
		update Property set name = ?, type = ? where id = ?
	"]]></string>
	<string name="query_property_delete"><![CDATA["
		delete from Property where id = ?
	"]]></string>
	<string name="query_property_delete_rooms"><![CDATA["
		delete from Room where property = ?
	"]]></string>
	<string name="query_property_types"><![CDATA["
	    select id as _id, name
	    from PropertyType
	    order by priority
	"]]></string>
	<string name="query_property_types_filtered"><![CDATA["
	    select id as _id, name
	    from PropertyType
	    where name like ?
	    order by priority
	"]]></string>
	<string name="query_room_types"><![CDATA["
	    select id as _id, name
	    from RoomType
	    order by priority
	"]]></string>
	<string name="query_room"><![CDATA["
		select id as _id, name, type
		from Room
		where id = ?
	"]]></string>
	<string name="query_rooms"><![CDATA["
		select
			r.id as _id,
			r.root,
			r.name,
			COALESCE(rt.image, rtk.image) as image
		from Room r
		join RoomType rt ON r.type = rt.id
		join RoomTypeKind rtk ON rt.kind = rtk.id
		where r.property = ?
		order by r.name asc 
	"]]></string>
	<string name="query_items"><![CDATA["
		select
			i.id as _id,
			i.name,
			c.name as category
		from Item     i
		join Category c ON i.category = c.id
		where i.parent = ?
		order by i.name asc 
	"]]></string>
	<string name="query_item"><![CDATA["
		select i.id as _id, i.name, c.name as category
		from Item i
		join Category c ON i.category = c.id
		where i.id = ?
	"]]></string>
</resources>
